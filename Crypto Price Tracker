<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Crypto Price Tracker (INR)</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #0d1117;
      color: #fff;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    .container {
      background: #161b22;
      padding: 25px;
      border-radius: 12px;
      width: 500px;
      text-align: center;
      box-shadow: 0 4px 10px rgba(0,0,0,0.6);
    }
    h2 {
      margin-bottom: 20px;
      color: #58a6ff;
    }
    table {
      width: 100%;
      border-collapse: collapse;
    }
    th, td {
      padding: 10px;
      border-bottom: 1px solid #333;
    }
    .green { color: #4caf50; }
    .red { color: #f44336; }
  </style>
</head>
<body>
  <div class="container">
    <h2>📈 Crypto Price Tracker (INR)</h2>
    <table>
      <thead>
        <tr>
          <th>Coin</th>
          <th>Price (₹)</th>
          <th>24h Change</th>
        </tr>
      </thead>
      <tbody id="cryptoTable">
        <tr><td colspan="3">Loading...</td></tr>
      </tbody>
    </table>
  </div>

  <script>
    async function fetchCryptoPrices() {
      try {
        // Popular coins: BTC, ETH, DOGE, LTC, ADA, BNB, SOL, XRP
        const url = "https://api.coingecko.com/api/v3/coins/markets?vs_currency=inr&ids=bitcoin,ethereum,dogecoin,litecoin,cardano,binancecoin,solana,ripple";
        const response = await fetch(url);
        const data = await response.json();

        const table = document.getElementById("cryptoTable");
        table.innerHTML = "";

        data.forEach(coin => {
          const row = `
            <tr>
              <td>${coin.name} (${coin.symbol.toUpperCase()})</td>
              <td>₹${coin.current_price.toLocaleString()}</td>
              <td class="${coin.price_change_percentage_24h >= 0 ? 'green' : 'red'}">
                ${coin.price_change_percentage_24h.toFixed(2)}%
              </td>
            </tr>
          `;
          table.innerHTML += row;
        });
      } catch (error) {
        console.error("Error fetching data:", error);
        document.getElementById("cryptoTable").innerHTML = `<tr><td colspan="3">Error fetching data!</td></tr>`;
      }
    }

    // Fetch immediately
    fetchCryptoPrices();
    // Update every 10 seconds
    setInterval(fetchCryptoPrices, 10000);
  </script>
</body>
</html>
